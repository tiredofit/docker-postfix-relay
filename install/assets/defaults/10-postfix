#!/command/with-contenv bash

ACCEPTED_NETWORKS=${ACCEPTED_NETWORKS:-"127.0.0.0/8 [::1]/128 172.16.0.0/12"}
BACKOFF_TIME_MAX=${BACKOFF_TIME_MAX:-1800}
BACKOFF_TIME_MIN=${BACKOFF_TIME_MIN:-450}
BOUNCE_QUEUE_LIFETIME=${BOUNCE_QUEUE_LIFETIME:-14}
DATA_LOCATION=${DATA_LOCATION:-"/data/"}
ENABLE_AUTH=${ENABLE_AUTH:-"TRUE"}
LIFETIME_QUEUE_BOUNCE=${LIFETIME_QUEUE_BOUNCE:-5}
LIFETIME_QUEUE_MAX=${LIFETIME_QUEUE_MAX:-14}
LIMIT_MESSAGE_RECIPIENTS=${LIMIT_MESSAGE_RECIPIENTS:-1000000}
LIMIT_MESSAGE_SIZE=${LIMIT_MESSAGE_SIZE:-26214400}
LOG_LEVEL_TLS_CLIENT=${LOG_LEVEL_TLS_CLIENT:-"1"}
LOG_SUMMARY=${LOG_SUMMARY:-"TRUE"}
QUEUE_DELAY=${QUEUE_DELAY:-900}
SERVER_NAME=${SERVER_NAME:-"example.com"}
SMTP_BANNER=${SMTP_BANNER:-"\$myhostname ESMTP NO UCE"}